name: publish
on: [push]
jobs:
  publish-hello-docker-image:
    runs-on: ubuntu-latest
    name: on_me
    steps:
    - uses: actions/checkout@v2
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build the hello-docker Docker image
      run: |
         docker build . --tag ghcr.io/andriish/betterci:latest
         docker run ghcr.io/andriish/betterci:latest
         docker push ghcr.io/andriish/betterci

  run-in-hello-docker-image:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
#    - name: Compile
#      id: compileindocker
#      uses: ./.github/workflows/mydocker
#    - name: Get the output status
#      run: exit ${{ steps.compileindocker.outputs.out }}

   
